import { SelectableRow } from './selectable-row.directive';

describe('SelectableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectableRow();
    expect(directive).toBeTruthy();
  });
});
